#include <stdio.h>
//#include <wiringPi.h>
//#include <softPwm.h>  
int main( void)
{
    int pwm_gpio5 = 1; 
    int i=0;
 /*
    wiringPiSetup();   
    pinMode(pwm_gpio5 ,PWM_OUTPUT);
    printf("pwm_gpio5 is blinking...\n");    
    for(;;)
    {
        for(i=0;i<1024;i++)
        {
            pwmWrite(1,i);
            delay(10);
            printf("Testing is %d......\n",i);    
        }
        for(i=1023;i>0;i--)
        {
            pwmWrite(1,i);
            delay(10);
            printf("Testing is %d......\n",i);    
        }
    }
    */
}